#1
my_list = [1, 2, 3]
#Вывод на экран: [1, 2, 3]

#Заменяем первый элемент списка
my_list[0] = 55
print(my_list)
#Вывод на экран: [55, 2, 3]; это произошло из-за замены 1 члена списка в строке 5 с использования замены нужно данного на другого, указали данное через [] и указания порядкого номера это члена начиная с 0

#2
my_tuple = (1, 2, 3)
#Вывод на экран: (1, 2, 3)

#Заменяем первый элемент списка
my_tuple[0] = 100
print(my_tuple)
#Вывод на экран: TypeError: 'tuple' object does not support item assignment; это подтверждает, что данные введенные класса tuple нельзя заменить

#3
my_string = "cat"
#Вывод на экран: cat

#Заменим данные
my_string[0] = 'b'
print(my_string)
#Вывод на экран: TypeError: 'str' object does not support item assignment; виды данных внесенные через класс строки неизменяются
