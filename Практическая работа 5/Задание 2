#Ввод параметров (вес, рост)
massa, rost = map(float, input("Введите свой вес в кг, рост в местрах:").split())

#Вычисление массы тела человека
IMT = massa/(rost**2)

#Вывод полученной массы тела на экран
print(f"Ваша масса тела: {IMT:.1f}")
